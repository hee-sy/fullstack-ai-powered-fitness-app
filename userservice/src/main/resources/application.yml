spring:
  application:
    # must match the yml filename stored in the configserver "resources/config/"
    name: user-service-application
  config:
    import: optional:configserver:http://localhost:8888